<UserControl
    x:Class="DS.Tools.Views.DashboardView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:DS.Tools.ViewModels"
    x:Name="Root"
    d:DesignHeight="650"
    d:DesignWidth="900"
    x:CompileBindings="True"
    x:DataType="vm:DashboardViewModel"
    mc:Ignorable="d">

    <UserControl.Styles>
        <!--  功能入口磁贴样式  -->
        <Style Selector="Button.toolTile">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="16,12" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        </Style>

        <Style Selector="Button.toolTile:pointerover">
            <Setter Property="Background" Value="{DynamicResource ActiveBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}" />
        </Style>

        <Style Selector="Button.toolTile:pressed">
            <Setter Property="Background" Value="{DynamicResource PrimaryHoverBrush}" />
        </Style>
    </UserControl.Styles>

    <!--  主页内容  -->
    <ScrollViewer HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="32" Spacing="24">
            <!--  标题  -->
            <TextBlock
                FontSize="28"
                FontWeight="Bold"
                Foreground="{DynamicResource TextPrimaryBrush}"
                Text="主页" />

            <!--  统计卡片（2x2）  -->
            <Grid ColumnDefinitions="*,*">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!--  当前时间卡片  -->
                <Border
                    Grid.Row="0"
                    Grid.Column="0"
                    Margin="0,0,8,16"
                    Padding="24"
                    Background="{DynamicResource CardBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="12">
                    <StackPanel>
                        <TextBlock
                            Margin="0,0,0,8"
                            HorizontalAlignment="Center"
                            FontSize="32"
                            Text="⏰" />
                        <TextBlock
                            HorizontalAlignment="Center"
                            FontSize="24"
                            FontWeight="Bold"
                            Foreground="{DynamicResource PrimaryBrush}"
                            Text="{Binding CurrentTime}" />
                        <TextBlock
                            Margin="0,4,0,0"
                            HorizontalAlignment="Center"
                            FontSize="12"
                            Foreground="{DynamicResource TextSecondaryBrush}"
                            Text="当前时间" />
                    </StackPanel>
                </Border>

                <!--  当前日期卡片  -->
                <Border
                    Grid.Row="0"
                    Grid.Column="1"
                    Margin="0,0,0,16"
                    Padding="24"
                    Background="{DynamicResource CardBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="12">
                    <StackPanel>
                        <TextBlock
                            Margin="0,0,0,8"
                            HorizontalAlignment="Center"
                            FontSize="32"
                            Text="📅" />
                        <TextBlock
                            HorizontalAlignment="Center"
                            FontSize="24"
                            FontWeight="Bold"
                            Foreground="{DynamicResource AccentSuccessBrush}"
                            Text="{Binding CurrentDate}" />
                        <TextBlock
                            Margin="0,4,0,0"
                            HorizontalAlignment="Center"
                            FontSize="12"
                            Foreground="{DynamicResource TextSecondaryBrush}"
                            Text="今日日期" />
                    </StackPanel>
                </Border>

                <!--  时间戳卡片  -->
                <Border
                    Grid.Row="1"
                    Grid.Column="0"
                    Margin="0,0,8,0"
                    Padding="24"
                    Background="{DynamicResource CardBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="12">
                    <StackPanel>
                        <TextBlock
                            Margin="0,0,0,8"
                            HorizontalAlignment="Center"
                            FontSize="32"
                            Text="🕐" />
                        <TextBlock
                            HorizontalAlignment="Center"
                            FontSize="24"
                            FontWeight="Bold"
                            Foreground="{DynamicResource AccentWarningBrush}"
                            Text="{Binding Timestamp}" />
                        <TextBlock
                            Margin="0,4,0,0"
                            HorizontalAlignment="Center"
                            FontSize="12"
                            Foreground="{DynamicResource TextSecondaryBrush}"
                            Text="当前时间戳（秒）" />
                    </StackPanel>
                </Border>

                <!--  功能总数卡片  -->
                <Border
                    Grid.Row="1"
                    Grid.Column="1"
                    Margin="0,0,0,0"
                    Padding="24"
                    Background="{DynamicResource CardBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="12">
                    <StackPanel>
                        <TextBlock
                            Margin="0,0,0,8"
                            HorizontalAlignment="Center"
                            FontSize="32"
                            Text="🧩" />
                        <TextBlock
                            HorizontalAlignment="Center"
                            FontSize="24"
                            FontWeight="Bold"
                            Foreground="{DynamicResource PrimaryBrush}"
                            Text="{Binding ToolCount}" />
                        <TextBlock
                            Margin="0,4,0,0"
                            HorizontalAlignment="Center"
                            FontSize="12"
                            Foreground="{DynamicResource TextSecondaryBrush}"
                            Text="功能总数" />
                    </StackPanel>
                </Border>
            </Grid>

            <!--  功能分组：按模块展示全部工具  -->
            <ItemsControl ItemsSource="{Binding ModuleGroups}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:DashboardModuleGroup">
                        <Border
                            Margin="0,0,0,8"
                            Padding="24"
                            Background="{DynamicResource CardBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="12">
                            <StackPanel Spacing="16">
                                <!--  模块标题  -->
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        FontSize="20"
                                        Text="{Binding ModuleIcon}" />
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        FontSize="18"
                                        FontWeight="SemiBold"
                                        Foreground="{DynamicResource TextPrimaryBrush}"
                                        Text="{Binding ModuleName}" />
                                </StackPanel>

                                <!--  工具磁贴（自动换行）  -->
                                <ItemsControl ItemsSource="{Binding Tools}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="vm:DashboardToolItem">
                                            <Button
                                                Margin="0,0,12,12"
                                                MinWidth="140"
                                                Classes="toolTile"
                                                Command="{Binding ElementName=Root, Path=DataContext.NavigateToToolCommand}"
                                                CommandParameter="{Binding NavigationId}">
                                                <StackPanel
                                                    Orientation="Horizontal"
                                                    Spacing="8">
                                                    <TextBlock
                                                        VerticalAlignment="Center"
                                                        FontSize="16"
                                                        Text="{Binding Icon}" />
                                                    <TextBlock
                                                        VerticalAlignment="Center"
                                                        Text="{Binding Name}" />
                                                </StackPanel>
                                            </Button>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
